# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Environment / secrets
.env
.env.*
!.env.example

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
# Backend config names an account-specific bucket; the example is committed.
backend.hcl
# IAM access-key exports (should never be near a repo, belt and braces)
*accessKeys*.csv
*_credentials.csv
# .terraform.lock.hcl is committed on purpose (pins provider versions).

# IDE / OS
.idea/
.vscode/
*.swp
Thumbs.db
.DS_Store

# Local tooling
.claude/
.agents/
skills-lock.json

# Private planning docs — never publish commercial/pricing strategy
docs/_private/

# Terraform saved plans can embed variable values
*.tfplan
